# server:
#   host: "127.0.0.1"
#   port: 8443
#   base_url: "https://10.0.0.2:8443"
#   caddyfile: "./configs/Caddyfile"


server:
  host: "h3.local"
  port: 8443
  base_url: "https://h3.local:8443"
  caddyfile: "./configs/Caddyfile"


network:
  interface: "veth-client"

  # # RTT sweep
  # rtt_ms: [0, 100, 200]
  # loss_pct: [0]
  # jitter_ms: [0]

  # # Loss sweep
  rtt_ms: [50]
  loss_pct: [0, 1, 3, 5]
  jitter_ms: [0]

  ## Jitter sweep
  # rtt_ms: [50]
  # loss_pct: [0]
  # jitter_ms: [0, 10, 20]


workloads:
  # many_small:
  #   num_objects: 300
  #   object_paths_file: "./configs/many_small.txt"
  few_large:
    num_objects: 10
    object_paths_file: "./configs/few_large.txt"

protocols:  #- "h3"
  - "h2"
  - "h3"

experiment:
  repetitions: 40
  cooldown_sec: 2
  output_csv: "./results/raw_results.csv"
  log_dir: "./results/logs"